<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{product.title}}</title>
    <style>
      .btn { background: #0073da; border: 1px solid #ccc; border-radius: 5px;
      box-shadow: 0 2px 3px rgba(10, 10, 10, 0.1), 0 0 0 1px rgba(10, 10, 10,
      0.1); color: #fff; cursor: pointer; font-size: 1.2rem; font-weight: bold;
      padding: 10px; text-transform: uppercase; text-decoration: none;
      font-family: sans-serif; transition: all 0.2s ease-in-out; } img { margin:
      2rem 0; } body { padding-top: 2rem; }
    </style>

  </head>
  <body>
    <span class="btn">Product added successfully!</span>
    <h1>{{product.title}}</h1>
    <p>{{product.price}}</p>
    <div><img src="{{product.thumbnail}}" alt="{{product.title}}" /></div>

    <a href="/products" class="btn">Back to Products</a>
  </body>
</html>